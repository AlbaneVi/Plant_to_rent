  <div class="col-xs-12 col-sm-6 col-sm-offset-3 col-md-4 col-md-offset-3">
  <div class="row">
      <h3><strong>
      Se connecter
      </strong></h3>
      <p id="text-lighter">Connectez-vous pour continuer sur Plant-to-rent.com</p>

      <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

        <div class="form-actions">
          <%= f.input :email,
                      required: false,
                      autofocus: true,
                      input_html: { autocomplete: "email"},
                      :placeholder => "capucine@gmail.com"
                      %>
          <%= f.input :password,
                      required: false,
                      input_html: { autocomplete: "current-password" },
                      :label => "Mot de passe",
                      :placeholder => "Mot de passe"
                      %>
          <%= f.input :remember_me, :id => 'case-remember',
                      :label => ' Se souvenir de moi',
                      as: :boolean if devise_mapping.rememberable?
                      %>
          <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
          <%= link_to "Mot de passe oublié ?", new_password_path(resource_name) %><br />
          <% end %>
          <br>
          <div class="form-actions button-cent">
            <%= f.button :submit, "Connexion", class: 'button', id: 'button-center'
             %>
            <% end %>
          </div>
        </div>
    </div>
  </div>

    <div class="col-xs-12 col-sm-6 col-sm-offset-3 col-md-4 col-md-offset-3">
  <div class="row">
      <br>
      <p>
        <strong>Vous n'avez pas encore de compte ?</strong>
      </p>
      <p id="text-lighter">
        Créez un compte facilement afin de profiter de tous nos services. La location de pantes en quelques clics !
      </p>
      <br>
      <div class="button-cent">
      <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
